function unhide(questions) {
    var item = document.getElementById(questions);
    if (item) {
            item=(item=='hidden')?'unhidden':'hidden';
        }
       // document.getElementById(startbutton) = 'hidden';
    }